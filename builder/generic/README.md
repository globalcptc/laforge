# LaForge Generic Builder

**NOTE: This Builder does nothing, it is an example of what basic things a builder might need and is used to skip all the build steps during deploy to just execute scripts**
